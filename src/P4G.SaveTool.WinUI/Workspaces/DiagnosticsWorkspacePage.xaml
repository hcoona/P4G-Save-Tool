<?xml version="1.0" encoding="utf-8"?>
<Page
  x:Class="P4G.SaveTool.WinUI.DiagnosticsWorkspacePage"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:automation="using:Microsoft.UI.Xaml.Automation"
  xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
  mc:Ignorable="d">

  <Border
    Padding="16"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
    BorderThickness="1"
    CornerRadius="8">
    <Grid RowSpacing="12">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" />
        <RowDefinition Height="Auto" />
        <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <TextBlock
        Text="Diagnostics / State"
        Style="{ThemeResource SubtitleTextBlockStyle}"
        TextWrapping="WrapWholeWords" />
      <TextBlock
        Grid.Row="1"
        Text="Review save validation results and current editor state messages."
        Style="{ThemeResource BodyTextBlockStyle}"
        Foreground="{ThemeResource TextFillColorSecondaryBrush}"
        TextWrapping="WrapWholeWords" />
      <Grid Grid.Row="2">
        <TextBlock
          x:Name="NoDiagnosticsTextBlock"
          Text="No diagnostics."
          Style="{ThemeResource BodyStrongTextBlockStyle}"
          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
          automation:AutomationProperties.AutomationId="NoDiagnosticsTextBlock" />
        <ListView
          x:Name="DiagnosticsListView"
          MinHeight="160"
          SelectionMode="None"
          Visibility="Collapsed"
          automation:AutomationProperties.AutomationId="DiagnosticsListView"
          automation:AutomationProperties.Name="Diagnostics">
          <ListView.ItemTemplate>
            <DataTemplate>
              <Grid Padding="0,6" RowSpacing="4" automation:AutomationProperties.Name="{Binding AutomationName}">
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid ColumnSpacing="8">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <TextBlock Text="{Binding Severity}" Style="{ThemeResource BodyStrongTextBlockStyle}" />
                  <TextBlock Grid.Column="1" Text="{Binding Code}" />
                  <TextBlock Grid.Column="2" Text="{Binding Target}" TextWrapping="Wrap" />
                </Grid>
                <TextBlock Grid.Row="1" Text="{Binding Message}" TextWrapping="Wrap" />
              </Grid>
            </DataTemplate>
          </ListView.ItemTemplate>
        </ListView>
      </Grid>
    </Grid>
  </Border>
</Page>
